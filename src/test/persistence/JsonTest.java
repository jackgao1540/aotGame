package persistence;

import model.Item;
import model.player.*;
import model.titans.*;

import java.util.ArrayList;

import static org.junit.jupiter.api.Assertions.*;

// DISCLAIMER: MOST OF THE CODE HERE WAS ADAPTED FROM THE PROVIDED REPOSITORY/PROJECT,
//             JsonSerializationDemo
public class JsonTest {
//    protected void checkPlayer(Player p1, Player p2) {
//        assertEquals(p1.getName(), p2.getName());
//        assertEquals(p1.getMoney(), p2.getMoney());
//        assertEquals(p1.getItems().size(), p2.getItems().size());
//        for (int i = 0; i < Math.min(p1.getItems().size(), p2.getItems().size()); i++) {
//            Item i1 = p1.getItems().get(i), i2 = p2.getItems().get(i);
//            assertEquals(i1.getName(), i2.getName());
//            assertEquals(i1.getPrice(), i2.getPrice());
//        }
//    }
//
//    protected void checkTitans(ArrayList<Titan> t1, ArrayList<Titan> t2) {
//        assertEquals(t1.size(), t2.size());
//        for (int i = 0; i < Math.min(t1.size(), t2.size()); i++) {
//            Titan i1 = t1.get(i), i2 = t2.get(i);
//            assertEquals(i1.getReward(), i2.getReward());
//        }
//    }
}
